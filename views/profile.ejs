<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
      <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10 ">

        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md py-2 px-3 text-sm mb-5 inline-block" href="/logout">logout</a>
        </div>

        <h3 class="text-3xl mb-5"><span class="font-light">hello</span>,<%= user.name %></h3>
        <h5 class="mb-5">you can create a new post.</h5>

        <form method="post" action="/post">
            <textarea placeholder="what's on your mind " class=" p-3 outline-none  w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"></textarea>
            <input class="px-3 py-2 w-42 bg-blue-500 rounded-md block mt-2" type="submit" value="create new post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">your posts</h3>
            <div class="postcontainer mt-5">
        
                <% user.posts.reverse().forEach(function(post){ %>
                            <div class="post w-1/3 p-4 border-2 border-zinc-800 bg-zinc-800 rounded-md mb-2">
                    <h4 class="text-blue-500 mb-2">@<%= user.username %> </h4>
                    <p class="text-tighter"><%= post.content %> </p>

                    <div class="btns flex gap-4 mt-5">
                        <a class="text-blue-500 " href="">Likes</a>
                        <a class="text-zinc-600" href="">edit</a>

                    </div>
                    
                    

                </div>
                
                    
            </div>
               <% })%>

        </div>
  
    </div>

</body>
</html>